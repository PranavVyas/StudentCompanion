<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vyas.pranav.studentcompanion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.RegistrationActivity" />
        <activity android:name=".dashboard.DashboardActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".prefences.SettingsActivity" />
        <activity android:name=".aboutapp.AboutAppActivity" />
        <activity android:name=".timetable.TimeTableActivity" />
        <activity android:name=".individualAttandance.IndividualAttendanceActivity"
            android:parentActivityName=".dashboard.DashboardActivity"></activity>
    </application>

</manifest>